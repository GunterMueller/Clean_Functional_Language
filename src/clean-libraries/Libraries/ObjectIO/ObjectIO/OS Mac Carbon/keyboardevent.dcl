definition module keyboardevent

import windowhandle, iostate

windowKeyboardIO :: !*(WindowStateHandle *(PSt .l)) !OSEvent !*(WindowHandles *(PSt .l)) !*(PSt .l)
	-> *(!Bool,!Maybe [Int],!Maybe DeviceEvent,!*PSt .l)
